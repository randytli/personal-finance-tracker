# Phase 2 M1 — Windows Tailscale name change (2026-09-25)

**Historical stage record:** The stop points below describe this action at its execution time. Phase 2 M1 is now owner-accepted and complete; see the [final rollout status](PFT_PHASE_2_M1_ROLLOUT_2026-09-24.md) and [successful restart/sign-in recovery](PFT_PHASE_2_M1_RESTART_RECOVERY_2026-09-25.md). The two remaining Phase 1 observations remain open.

**Status: name change complete; stopped before HTTPS and Serve.** The owner directed a separate minimal rename from `randy-pc` to `pft-host` before any certificate is requested. [Tailscale documents](https://tailscale.com/docs/features/magicdns) that a machine-name change updates its MagicDNS name. No Windows OS hostname, tailnet policy, Production app, or Docker service was changed.

## Executed action and verification

- Preflight Windows Tailscale CLI `1.102.4`: node ID `3351434150137017` (`nUNEQYVsAT11CNTRL`), IPv4 `100.109.100.36`, IPv6 `fd7a:115c:a1e0::ea01:64cd`, DNS `randy-pc.tailc4d964.ts.net`, MagicDNS enabled, and `CertDomains: null`. The iPhone remained `iphone-15-pro.tailc4d964.ts.net` at `100.99.41.95`. `serve status --json` and `funnel status --json` returned `{}`; Windows `http://127.0.0.1:3000/` returned `200`. The saved restrictive policy targets device IPs, not the DNS name: only iPhone `100.99.41.95` → Windows `100.109.100.36` TCP 443, with two policy tests. No policy edit was made.
- Ran exactly `& 'C:\Program Files\Tailscale\tailscale.exe' set --hostname=pft-host` on the Windows client. The installed CLI says `tailscale set` changes only explicitly supplied preferences; the command exited 0.
- Afterward, Windows Tailscale reported `HostName: pft-host` and `DNSName: pft-host.tailc4d964.ts.net.` with the **same node ID, user ID, IPv4, IPv6, and online iPhone identity**. Windows `Resolve-DnsName -Name 'pft-host.tailc4d964.ts.net' -Type A` returned `100.109.100.36`. MagicDNS remains enabled and `CertDomains` remains null. Serve and Funnel statuses remain `{}`; Windows localhost home and `/api/pft/sync/status` both returned `200`. Docker still shows the same four `pft-runtime` services up, with API/web/DB healthy, jobs running, only web published on `127.0.0.1:3000`, and no API/DB host port.

The IP-based policy text/tests remain as previously saved; policy evaluation was not changed by this rename. In the saved policy, `pft-windows` is an alias for `100.109.100.36`, not a MagicDNS name. The two tests still refer to that same alias/IP and need no edit or policy save. No fresh Admin Console policy save/test run occurred during the rename. **At this rename stop point**, the Production API environment still contained the old browser Origin `https://randy-pc.tailc4d964.ts.net`; no Production configuration or service restart was part of the rename. Serve was disabled. The guarded [M1 HTTP helper](../scripts/pft_m1_http_env.py) was prepared for the separate Origin action recorded below.

## Rollback and stop point

If the Tailscale name itself must be reverted, run `& 'C:\Program Files\Tailscale\tailscale.exe' set --hostname=Randy_PC`, then verify node ID/IP, `randy-pc.tailc4d964.ts.net` resolution, Serve/Funnel `{}`, and localhost `200` again. This rollback has not been needed or run. Do not change the restrictive IP-based policy as part of a name rollback.

**2026-09-25 continuation:** After the rename stop point, the owner separately approved and completed the [exact Production API Origin replacement](PFT_PHASE_2_M1_ORIGIN_REPLACEMENT_ACTION_2026-09-25.md) and [HTTPS prerequisite](PFT_PHASE_2_M1_HTTPS_PREREQUISITE_ACTION_2026-09-25.md) for `pft-host.tailc4d964.ts.net`. The HTTPS receipt records a stale former-name Serve configuration that appeared and was cleared. The final state is HTTPS capability enabled, current-name machine certificate status **No certificate found**, and Serve/Funnel `{}`. No Windows restart, manual financial write, or manual Plaid call was made. Windows restart/sign-in remains a separate later boundary.
