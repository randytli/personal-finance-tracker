# Phase 2 M1 — Windows restart/sign-in recovery execution (2026-09-25)

**Status: accepted M1 automatic restart/sign-in recovery; no manual recovery needed.** The owner approved the exact [restart action packet](PFT_PHASE_2_M1_WINDOWS_RESTART_ACTION_2026-09-25.md). No clock, cursor, schedule, Plaid, financial data, policy, or Serve configuration was changed by this action.

## Gate A, 2026-09-25 18:15–18:19 UTC

- Checkout: `main` at `79bc3a8add6ff5a2aa0def0c70f383cf503c9184`; existing uncommitted M1 code/evidence was preserved. Windows boot time before the test: `2026-09-21 10:49:01` local. Docker Desktop Windows-native status was `running` with session `9caf2911-65e7-4864-9c38-b739c6cbe599`. The owner confirmed both Docker Desktop's **Start when you sign in** setting and Windows Startup Apps enabled. Saved `AutoStart=True`; Windows StartupApproved Run value `000000000000000000000000`. Tailscale service was Running/Automatic, CLI 1.102.4.
- Tailscale backend Running; Windows node ID `3351434150137017`, `pft-host.tailc4d964.ts.net.` → `100.109.100.36`, iPhone `iphone-15-pro.tailc4d964.ts.net.` → `100.99.41.95`, both online. MagicDNS enabled, current certificate domain exactly `pft-host.tailc4d964.ts.net`; Windows DNS resolved that name to the same IP. Serve JSON contained only HTTPS `443`, current-name `/` handler proxying to `http://127.0.0.1:3000`. Funnel text said **tailnet only**; JSON had no `AllowFunnel` flag. The owner read the Admin Console and confirmed MagicDNS/HTTPS Certificates, valid current-name certificate, and the one iPhone → Windows `tcp:443` grant with the same two tests and no other grants/ACL/SSH. The preserved policy JSON SHA-256 remains `a13a5dfa372facd261255ea2e26e4270ed4ad0202604e9c602a54450081b0428`; no policy was saved.
- The four existing `pft-runtime` containers were running with `unless-stopped`: DB `3b97f7733793` / image `sha256:c1b37833...`, API `f4799bf8162b` / `sha256:cec3537e...`, web `643d75993526` / `sha256:428dec07...`, jobs `4bc21502f34b` / `sha256:f3e3536c...`. DB/API/web were healthy; jobs running. The old backfill DB was exited with restart `no`; `pft-runtime-db-1` alone mounted `personal-finance-tracker_pgdata_production_backfill`. Only web published `127.0.0.1:3000`; API/Production DB had no host port. The private env file was mode `0600`, and its four HTTP values exactly matched the running API: strict local HTTP, local allowed Origin, `api:8000,127.0.0.1:8000` Hosts, and localhost/current-name HTTPS Origins. No other private values were printed.
- Windows localhost home/status returned `200/200`. Harmless nonexistent review POST probes returned `404` for `Origin: https://pft-host.tailc4d964.ts.net`, `403` for the old HTTPS Origin, and `403` with no Origin. Sync status showed five active institutions, no active run, jobs running with fresh heartbeat, backup healthy with last success `2026-09-24T20:08:24.809645Z`, and publication `3795ad32-ca11-4695-bc06-9cbfcdd0f51d` at `2026-09-24T20:08:30.619006Z`.
- The protected M0 recovery archive `pft-phase2-m0-20260924T222654Z.dump` exists; SHA-256 `439ec74f736e8e5536e4b554ffb8800bbdc757de7062ec400e18e3af5e924775`, `pg_restore -l` passed, and Windows ACLs grant full access only to the owner, Administrators, and SYSTEM. Its [accepted isolated restore](PFT_PHASE_2_M0_BASELINE_2026-09-24.md) covers the current published baseline, which has not changed since that restore.
- The read-only repeatable-read [immediate pre-restart fingerprint](PFT_PHASE_2_M1_RESTART_BEFORE_2026-09-25.json) has SHA-256 `3ab2b7da32c7cf6b058ebe8cdd7c34a75787951e5c2e00c676e9608af88f10ac`. Compared with the accepted post-Serve fingerprint, database identity, five-institution scope, raw source breakdown, all 15 table counts, 14 non-heartbeat table hashes, and zero integrity checks match; only `sync_runtime_state` heartbeat changed. Desktop localhost September Overview gross/refunds/reimbursements/benefits/net were `6183.89 / 1607.97 / 37.50 / 200.26 / 4338.16`. Trailing-12-month Membership through August had 113 transactions and gross/refunds/benefits/net `2636.67 / 10.65 / 438.02 / 2188.00`. Needs Review was 2; Credits & Transfers 504; one Review transaction detail was read without mutation. Publication was the ID above.

**Gate A decision:** Pass. The second naturally elapsed Phase 1 daily-cycle observation and a real overdue catch-up observation remain open; this short restart is not proof of either without real elapsed due-time evidence.

## Gate B — one normal Windows restart and sign-in

- Ran exactly `& 'C:\Windows\System32\shutdown.exe' /r /t 0` once, without `/f`. The owner signed back into the same Windows account and was instructed to leave Docker Desktop/Tailscale to start automatically. No Docker, Tailscale, Serve, or container manual start command was issued before Gate C observations.
- Windows boot time advanced from `2026-09-21 10:49:01` local to `2026-09-25 14:20:45` local (`18:20:45 UTC`). At `18:22:57 UTC`, before any manual recovery, Tailscale service was Running/Automatic and Docker Desktop Windows-native status was `running` with new session `d710940f-d2a2-400a-8a0b-3bfcee6f357d`. Docker showed all four Production containers already running; their post-boot start times were about `18:21:53 UTC`.

## Gate C — automatic recovery and preservation

- Docker automatically resumed the **same** container and image IDs as Gate A for DB, API, web, and jobs; DB/API/web were healthy and jobs running. The old backfill DB stayed exited with restart `no`; `pft-runtime-db-1` remained the sole running mount owner of `personal-finance-tracker_pgdata_production_backfill`. Web still published only `127.0.0.1:3000`; Production API/DB still had no host port. No image rebuild, container recreation, or manual Docker start occurred.
- Tailscale backend was Running with the same Windows node ID `3351434150137017`, `pft-host.tailc4d964.ts.net.` → `100.109.100.36`, online iPhone `iphone-15-pro.tailc4d964.ts.net.` → `100.99.41.95`, MagicDNS enabled and exact current-name certificate domain. Serve automatically retained only the HTTPS `443` current-name `/` → `http://127.0.0.1:3000` handler. `funnel status` reported **tailnet only**, with no `AllowFunnel` flag. Windows DNS resolved the current name to the same IP. The owner read Admin Console after sign-in and confirmed the valid certificate, HTTPS/MagicDNS, and unchanged sole iPhone → Windows `tcp:443` policy grant/two tests with no other grants/ACL/SSH. No manual Tailscale/Serve reconfiguration occurred.
- Windows localhost home/API remained `200/200` immediately and on the final check at `18:26:10 UTC`. The harmless proxy Origin probes remained `404` current HTTPS Origin, `403` former hostname, `403` missing Origin. Status at `18:23:55 UTC` showed jobs had already written a fresh post-boot heartbeat and were `running`; backup remained `healthy` with the same last success `2026-09-24T20:08:24.809645Z`. Five Items remained active; no sync was active; publication stayed `3795ad32-ca11-4695-bc06-9cbfcdd0f51d`. No scheduled run or backup was artificially triggered, and this short reboot did not cross the next due time.
- The read-only repeatable-read [post-restart fingerprint](PFT_PHASE_2_M1_RESTART_AFTER_2026-09-25.json) has SHA-256 `96e8bd3b795c90caaee5412c6359291a25d4bcadfa941481ab980e65877c7682`. Against the immediate pre-restart fingerprint, database identity, five-institution scope, raw source breakdown, all 15 table counts, 14 non-heartbeat table hashes, and all four zero-integrity checks match exactly. Only `sync_runtime_state` advanced with its row count unchanged, matching the observed jobs heartbeat. Desktop localhost September Overview remained `6183.89 / 1607.97 / 37.50 / 200.26 / 4338.16` gross/refunds/reimbursements/benefits/net; Membership through August remained 113 transactions and `2636.67 / 10.65 / 438.02 / 2188.00` gross/refunds/benefits/net; Needs Review stayed 2 and Credits & Transfers 504. A Review detail was available after restart, and the complete financial-table hashes preserved its underlying rows.
- On real iPhone Safari, the owner confirmed the exact HTTPS hostname with no TLS warning and the same Overview, Membership, both Review modes, one detail, publication, reload, and focus behavior on **Wi-Fi and cellular**. With cellular on and Tailscale VPN off, a fresh status URL failed to load; after reconnecting Tailscale, the same URL loaded. Final Serve status still showed the singleton current-name handler and Funnel **tailnet only**.

**M1 recovery result: pass.** Docker Desktop, containers, Tailscale, and private Serve recovered after the single Windows restart/sign-in without manual reconfiguration. Localhost, restricted iPhone access, jobs/backup health, and financial preservation passed. No conditional recovery/rollback command was used. The remaining Phase 1 second normal daily cycle and real overdue startup catch-up observation are still open and were not manufactured or certified by this short restart. Stop here; M2 has not begun.
