# Phase 2 M0 baseline and safety gate — 2026-09-24

**Status:** M0 evidence complete; awaiting owner acceptance before M1. Phase 1 M6 remains open for one more naturally elapsed daily cycle and a future real overdue catch-up observation. This audit did not stop jobs or change Production data.

## Code and runtime identity

- At audit start and after verification, checkout was `main` at `74681826f33fda110c0de496031db7eec99f7d72`, equal to `origin/main` and the Phase 2 plan's pinned code baseline. The only pre-existing uncommitted path was the untracked `docs/PFT_PHASE_2_ARCHITECTURE_PLAN.md`. No tracked source diff existed. M0 added only this report and its sanitized [fingerprint](PFT_PHASE_2_M0_FINGERPRINT_2026-09-24.json). The temporary TypeScript build-info change from typechecking was restored.
- Docker Compose project `pft-runtime` runs healthy `db`, `api`, and `web` containers and a running `jobs` container. Image IDs at inspection: DB `c1b3783309b6`, API `9ef45ea6154a`, web `428dec07c99f`, jobs `f3e3536c901a`. Jobs reports `PFT_APP_COMMIT=2b413550433e83db151ef1f62ad98b26d70fbd9e`, the M5 implementation commit. Later repository commits include Phase 1 acceptance documentation; the checkout's `7468182` is therefore newer than the running images without evidence of a code/runtime mismatch requiring deployment in M0.
- PostgreSQL is 16.15, database `pft_production_backfill`, role `pftbackfill`. `pft-runtime-db-1` is the **sole running mount owner** of `personal-finance-tracker_pgdata_production_backfill` at `/var/lib/postgresql/data` (Docker source `/var/lib/docker/volumes/personal-finance-tracker_pgdata_production_backfill/_data`). It has `unless-stopped` restart policy and no published port. The old `personal-finance-tracker-db-production-backfill-1` remains exited, mounts the same volume only while stopped, and has restart policy `no`. Pilot and development PostgreSQL containers use distinct volumes; the pilot is loopback-published at 5433 and the development DB is published at 5432. Neither is the Production-volume owner.
- API and jobs take private environment files through `compose.runtime.yml`; the DB uses the private DB env file and jobs also mounts the protected Windows backup directory. The three `.env.runtime.*.production.local` files are mode 0600. `api` and `jobs` have matching configured DB identity keys, and the API's explicit read-only database-name and runtime-column checks passed. Only web publishes `127.0.0.1:3000`; API and Production DB have no host port. No secret, access token, or cursor value is recorded here.

## Snapshot, schema, and financial preservation

- The existing `scripts/pft_m6_fingerprint.py` captured a UTC, repeatable-read, read-only snapshot at **2026-09-24 22:24:13 UTC**. The linked JSON records every column's full-row hash for all **15 public application tables** (156 columns), institution/account scope, raw source/date/removal breakdown, and four integrity checks. It contains counts and hashes, not stored rows, token values, or cursor values. There is no general schema-version ledger. A schema-only extraction from the fresh M0 dump has SHA-256 `155d8dc2bd9b19090f38b1ba47b8924b5843daf6a2a3edfd6a55032e62742c72`; the matching isolated restore had 40 constraints and 32 indexes. The explicit `api.migrate_once` path and its `migrate_sync_runs` helper were applied at the Phase 1 M6 cutover; current `consumer_scope_migrations` contains version 1. Runtime verification alone was not used as a substitute for catalog inspection.
- Scope remains one user, five active/unpaused Items, five present cursors, and 13 accounts: American Express 2 enabled, Bank of America 1, Capital One 2, Chase 3, Robinhood 1 enabled plus 4 disabled investment accounts. Cursor presence and truncated digests were checked without printing cursor contents; all five retry counts are zero and no retry is scheduled.
- The snapshot has 2,615 raw and 2,615 normalized rows. Raw rows are 2,604 active and 11 removed; 171 are statement sourced. The single statement batch remains applied with 171 rows. Override rows are classification **83**, category **177** (4 cleared), label **28** (13 cleared), and benefit **17**. Full-row hashes include cleared and audit columns. The four fingerprint integrity checks—raw Item/account, normalized/raw account, statement evidence, and Plaid/statement linkage—are all zero. Account, consumer migration, legacy consumer, statement-batch, and statement-row hashes equal the September 23 Phase 1 post-recovery fingerprint.
- Compared with the September 23 baseline, the five extra raw/normalized rows and five additional removed flags align with the **naturally scheduled** September 24 run: five additions and five removals across its five Item outcomes. The extra sync run and Item-run rows are expected. Item hashes changed with last-success/cursor state. Override counts increased from 60/166/27/8 to 83/177/28/17. Their audit timestamps show 23 classification, 11 category, 1 label, and 9 benefit rows updated after the prior recovery snapshot; these are intervening application/user edits, not a migration or M0 write. The count and timestamp evidence does not attribute every monthly-dollar delta to a particular edit, so the old financial totals are not presented as the current baseline.
- Current read-only proxied monthly results (gross / refunds / reimbursements / benefits / net) are June `17563.46 / 114.03 / 5000.00 / 223.46 / 12225.97`, July `5470.42 / 474.78 / 0.00 / 128.56 / 4867.08`, August `5807.56 / 810.50 / 366.00 / 232.26 / 4398.80`, and September `6183.89 / 1607.97 / 37.50 / 200.26 / 4338.16`. Each obeys `net = gross - refunds - reimbursements - benefits`. September's four institution groups and seven account groups each sum to `4338.16`; category net sums to `4538.42`, which includes the separately shown `200.26` of benefits under the current API semantics. Trailing-12-month Membership through August has 113 transactions, gross `2636.67`, refunds `10.65`, benefits `438.02`, and net `2188.00`, which reconciles. Current unclassified count is zero for these representative months.

## Publication, backup, and recovery

- The latest published run is `3795ad32-ca11-4695-bc06-9cbfcdd0f51d`, triggered by jobs at `2026-09-24 20:08:25.081836 UTC` and successfully published at `20:08:30.619006 UTC`, with successful classification of 2,564 rows. All five Item outcomes are successful/published, one page, zero retries or errors: American Express 2 added/1 removed; Bank of America 0/0; Capital One 0/0; Chase 1/1; Robinhood 2/3. Pending manual requested/handled sequences are both zero and no sequence is running. The status endpoint showed jobs running, backup healthy, and the same publication marker; heartbeat at observation was `2026-09-24 22:28:37 UTC`.
- The normal daily backup was successful at `2026-09-24 20:08:24.809645 UTC`, **before** the run started. Its host archive `pft-daily-20260924T200824837238Z.dump` and manifest agree on 488,600 bytes and SHA-256 `2c7b254d03e235949c4cbe0d50cbe9475ef25693b88e2b22be1e70f145491503`; no backup error was recorded. This backup is pre-sync, so the September 23 restored checkpoint and this daily archive alone do not cover the post-sync M0 baseline.
- A fresh read-only custom-format dump was therefore taken at **22:26:54 UTC** and restored with `--exit-on-error --no-owner --no-privileges` into a newly initialized PostgreSQL 16 cluster under `/tmp` on port 55439, database `pft_m0_recovery_20260924`. The dump is 452,798 bytes with SHA-256 `439ec74f736e8e5536e4b554ffb8800bbdc757de7062ec400e18e3af5e924775`; archive listing passed. All 14 non-heartbeat table counts and full-row hashes, institution/source breakdowns, and integrity checks matched the earlier Production repeatable-read snapshot exactly. `sync_runtime_state` differs only because jobs continued updating its heartbeat between snapshots; this is expected moving-source operation, not a recovery mismatch. The existing local key decrypted all five encrypted tokens in the **isolated restore** without printing plaintext or making a Plaid call. The independently retrieved recovery key was separately demonstrated in the accepted Phase 1 M0 evidence; independent retrieval was not repeated in this audit.
- A checksum-identical copy is at `C:\Users\tianr\PFTBackups\Production\pft-phase2-m0-20260924T222654Z.dump`. Windows ACL inspection grants full access only to the owner, Administrators, and SYSTEM. The prior Phase 1 report records owner-verified C: encryption and independent encrypted USB recovery. This new M0 dump has not itself been copied to external media; the existing automated daily host backup and prior external recovery evidence remain distinct. The isolated restore excludes roles/grants by design, so full disaster recovery still follows the documented role/config/key procedure. The isolated PostgreSQL cluster was stopped after verification; its data files remain under `/tmp` for review.

## Verification and remaining Phase 1 evidence

- Backend: **235 tests passed, zero skips**, with all seven configured synthetic PostgreSQL opt-ins against the newly created `pft_m0_synthetic_20260924` database on loopback port 55439, `PLAID_ENV=sandbox`, and synthetic user ID. Frontend: **28 tests passed**, TypeScript `--noEmit` passed, and Next.js production build passed. Python compilation and `git diff --check` passed. The build emitted existing Next.js metadata/viewport warnings; no test or build failure occurred. No tests targeted Production.
- Of the **two** subsequent normal daily cycles left open by Phase 1 M6, **the first (September 24) now has observed evidence**: backup-before-sync, five successful Item outcomes, publication marker, healthy status, and the financial reconciliations above. **The second remains open** until it occurs naturally and the same checks are made. A real overdue startup catch-up after downtime remains unobserved; the short September 23 restart/sleep/sign-in tests did not create one. No clock, cursor, or schedule was manipulated for this report. Phase 1 M6 final acceptance is therefore still open; M0 acceptance can precede it under the Phase 2 plan. M8 authority migration and local-host retirement require closure or a separately reviewed replacement acceptance protocol.

**Gate assessment:** Database and volume identity, preservation, isolated recovery, and current tests pass for M0. No unexplained integrity error or Production-volume ownership conflict was found. Stop for owner acceptance of this report before M1. No Production SQL write, Production Plaid call, service restart, migration, network installation, cloud resource creation, or M1+ implementation was performed.
